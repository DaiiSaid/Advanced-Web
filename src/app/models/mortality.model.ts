export class Mortality {
    id: number;
    cause: string;
    count: number;
    // Add other properties as needed
  
    constructor() {
      // Initialize default values if needed
      this.id = 0;
      this.cause = '';
      this.count = 0;
    }
  }
  